---
phase: 01-marketing-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(marketing)/page.tsx
  - src/components/marketing/hero-section.tsx
  - src/components/marketing/hero-animated-demo.tsx
  - src/components/marketing/value-props-grid.tsx
  - src/components/marketing/pricing-teaser.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-05

must_haves:
  truths:
    - "Visitor lands on / and reads 'Komponen kreatif untuk developer Indonesia' as the display headline"
    - "Visitor sees two CTAs: 'Mulai Sekarang' → /pricing (primary) and 'Lihat Contoh' → /explore (secondary, stubbed)"
    - "A looping CSS-only animated demo plays inside an aspect-[16/9] container below the CTAs"
    - "Three value-prop columns render side-by-side on desktop and stack on mobile"
    - "A pricing teaser section with 'Lihat semua paket →' link appears above the footer"
    - "All copy is Bahasa Indonesia matching UI-SPEC.md strings verbatim"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Home page composing HeroSection + ValuePropsGrid + PricingTeaser"
    - path: "src/components/marketing/hero-section.tsx"
      provides: "Hero with headline, sub-headline, two CTAs, and HeroAnimatedDemo"
    - path: "src/components/marketing/hero-animated-demo.tsx"
      provides: "CSS-only looping magnetic hover animation (Server Component, zero JS)"
    - path: "src/components/marketing/value-props-grid.tsx"
      provides: "3-column value props (1-col on mobile)"
    - path: "src/components/marketing/pricing-teaser.tsx"
      provides: "Bottom-of-home pricing nudge with link to /pricing"
  key_links:
    - from: "src/app/(marketing)/page.tsx"
      to: "src/components/marketing/hero-section.tsx"
      via: "import + JSX render"
      pattern: "HeroSection"
    - from: "src/components/marketing/hero-section.tsx"
      to: "src/components/marketing/hero-animated-demo.tsx"
      via: "import + JSX render"
      pattern: "HeroAnimatedDemo"
    - from: "src/components/marketing/hero-animated-demo.tsx"
      to: "src/app/globals.css"
      via: "CSS class names hero-demo-cursor, hero-demo-button"
      pattern: "hero-demo-cursor"
---

<objective>
Build the home page: hero section with CSS-only animated demo, value props grid, and pricing teaser.

Purpose: MKT-01 — the home page must communicate the value proposition and drive visitors toward /pricing. This is the brand's first impression and the primary conversion funnel entry point.

Output: A complete, mobile-responsive home page with display headline in Bahasa Indonesia, two CTAs, a looping CSS-only animated demo (zero JS bundle), a 3-column value props grid, and a pricing teaser section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-marketing-surface/01-UI-SPEC.md
@.planning/phases/01-marketing-surface/01-PATTERNS.md
@.planning/phases/01-marketing-surface/01-01-SUMMARY.md

<interfaces>
<!-- Contracts established by Plan 01 that this plan builds on -->

From src/app/globals.css (added by Plan 01 — hero animation classes):
```css
/* These classes MUST exist before hero-animated-demo.tsx references them */
.hero-demo-cursor {
  animation: orbit-cursor 4s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}
.hero-demo-button {
  animation: button-pulse 4s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}
/* @keyframes orbit-cursor and button-pulse also defined in globals.css */
```

From src/app/(marketing)/layout.tsx (Plan 01):
```tsx
// Children of this layout receive: sticky TopNav, main#main, Footer
// No re-wrapping needed — page.tsx just returns sections directly
```

Typography class pattern (from 01-PATTERNS.md Shared Patterns):
```
Display (hero h1): text-[3rem] lg:text-[6rem] font-semibold leading-[1.05] tracking-[-0.03em]
Heading (h2):      text-[1.75rem] lg:text-[2.5rem] font-semibold leading-[1.15] tracking-[-0.02em]
Body:              text-base font-normal leading-[1.5]
Label:             text-sm font-normal leading-[1.45]
Muted:             text-neutral-500
FORBIDDEN:         font-medium (500), font-bold (700), italic
```

Button states (from UI-SPEC Interaction States):
```
Primary CTA (Mulai Sekarang):
  Resting: bg-neutral-950 text-white px-6 py-3 rounded-lg font-semibold text-base
  Hover: hover:bg-neutral-800 transition-colors duration-150 ease-out
  Focus: focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950
  Active: active:bg-neutral-900

Secondary CTA (Lihat Contoh):
  Resting: bg-transparent text-neutral-950 border border-neutral-200 px-6 py-3 rounded-lg font-semibold text-base
  Hover: hover:border-neutral-950 transition-colors duration-150 ease-out
  Active: active:bg-neutral-50
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HeroAnimatedDemo and HeroSection components</name>
  <read_first>
    - .planning/phases/01-marketing-surface/01-UI-SPEC.md (Hero Animation Spec section, Home page layout spec, hero copy contract)
    - .planning/phases/01-marketing-surface/01-PATTERNS.md (hero-animated-demo.tsx pattern, hero-section.tsx pattern)
    - src/app/globals.css (confirm hero-demo-cursor and hero-demo-button classes exist from Plan 01)
  </read_first>
  <files>
    src/components/marketing/hero-animated-demo.tsx,
    src/components/marketing/hero-section.tsx
  </files>
  <action>
**Create src/components/marketing/hero-animated-demo.tsx** — Server Component (NO `'use client'`). Pure JSX + references to CSS classes in globals.css. Zero JS shipped to client.

```tsx
// Server Component — no 'use client'
export function HeroAnimatedDemo() {
  return (
    <div
      role="img"
      aria-label="Demo animasi tombol magnetic hover"
      className="relative w-full max-w-[960px] mx-auto aspect-video border border-neutral-200 rounded-[12px] bg-white overflow-hidden flex items-center justify-center"
      style={{ padding: "64px" }}
    >
      {/* Orbiting cursor dot */}
      <div
        className="hero-demo-cursor absolute w-3 h-3 rounded-full bg-neutral-950 pointer-events-none"
        style={{ top: "50%", left: "50%", marginTop: "-6px", marginLeft: "-6px" }}
        aria-hidden="true"
      />

      {/* Scaling pill button */}
      <div
        className="hero-demo-button relative z-10 bg-neutral-950 text-white text-sm font-semibold px-6 py-3 rounded-full select-none pointer-events-none"
        aria-hidden="true"
      >
        Hover me
      </div>

      {/* Caption — only visible when reduced motion is off; replaced by CSS for reduced-motion */}
      <p className="absolute bottom-4 left-0 right-0 text-center text-sm text-neutral-500 leading-[1.45]">
        Live demo — salin kode dalam satu klik
      </p>
    </div>
  );
}
```

**Create src/components/marketing/hero-section.tsx** — Server Component (NO `'use client'`). Uses `<Link>` for CTAs. All copy verbatim from UI-SPEC.

Exact structure (per UI-SPEC Home page layout):
```tsx
import Link from "next/link";
import { HeroAnimatedDemo } from "@/components/marketing/hero-animated-demo";

export function HeroSection() {
  return (
    <section className="px-4 sm:px-8 lg:px-12 pt-24 pb-24 lg:pt-32 lg:pb-32">
      <div className="max-w-[1200px] mx-auto">
        {/* Headline + CTAs */}
        <div className="max-w-[840px]">
          <h1 className="text-[3rem] lg:text-[6rem] font-semibold leading-[1.05] tracking-[-0.03em] text-neutral-950">
            Komponen kreatif untuk developer Indonesia
          </h1>
          <p className="mt-6 text-base font-normal leading-[1.5] text-neutral-950 max-w-[52ch]">
            Animasi siap pakai, lima format kode, satu langganan. Buat developer yang nggak mau bikin ulang interaksi yang sama tiap project.
          </p>
          <div className="mt-8 flex flex-col sm:flex-row gap-3 sm:gap-4">
            {/* Primary CTA */}
            <Link
              href="/pricing"
              className="inline-flex items-center justify-center bg-neutral-950 text-white px-6 py-3 rounded-lg text-base font-semibold hover:bg-neutral-800 active:bg-neutral-900 transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950"
            >
              Mulai Sekarang
            </Link>
            {/* Secondary CTA — stubbed (Phase 3 wires /explore) */}
            <Link
              href="/explore"
              className="inline-flex items-center justify-center bg-transparent text-neutral-950 border border-neutral-200 px-6 py-3 rounded-lg text-base font-semibold hover:border-neutral-950 active:bg-neutral-50 transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950"
            >
              Lihat Contoh
            </Link>
          </div>
        </div>

        {/* Animated demo */}
        <div className="mt-12">
          <HeroAnimatedDemo />
        </div>
      </div>
    </section>
  );
}
```

**Important:** Both components are Server Components. Do NOT add `'use client'`. The animation is purely CSS — `hero-demo-cursor` and `hero-demo-button` classes are already defined in `globals.css` by Plan 01. No JS, no framer-motion, no Lottie.
  </action>
  <verify>
    <automated>grep -n "use client" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-animated-demo.tsx; grep -n "Komponen kreatif" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-section.tsx && grep -n "Mulai Sekarang" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-section.tsx && grep -n "Lihat Contoh" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-section.tsx && grep -n "hero-demo-cursor" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-animated-demo.tsx && grep -n "orbit" /Users/calvinlai/Desktop/lailit.supply/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "use client" src/components/marketing/hero-animated-demo.tsx` returns ZERO lines (must be Server Component)
    - `grep "use client" src/components/marketing/hero-section.tsx` returns ZERO lines (must be Server Component)
    - `grep "hero-demo-cursor" src/components/marketing/hero-animated-demo.tsx` returns a match
    - `grep "hero-demo-button" src/components/marketing/hero-animated-demo.tsx` returns a match
    - `grep "role=\"img\"" src/components/marketing/hero-animated-demo.tsx` returns a match (accessibility)
    - `grep "aria-label.*Demo animasi" src/components/marketing/hero-animated-demo.tsx` returns a match
    - `grep "Komponen kreatif untuk developer Indonesia" src/components/marketing/hero-section.tsx` returns a match (verbatim headline)
    - `grep "Animasi siap pakai" src/components/marketing/hero-section.tsx` returns a match (verbatim sub-headline)
    - `grep "Mulai Sekarang" src/components/marketing/hero-section.tsx` returns a match
    - `grep 'href="/pricing"' src/components/marketing/hero-section.tsx` returns a match (primary CTA destination)
    - `grep "Lihat Contoh" src/components/marketing/hero-section.tsx` returns a match
    - `grep "aspect-video" src/components/marketing/hero-animated-demo.tsx` returns a match (16/9 container)
  </acceptance_criteria>
  <done>HeroAnimatedDemo is a pure Server Component with CSS-only magnetic orbit animation; HeroSection has display headline in Bahasa Indonesia, sub-headline, primary CTA to /pricing, secondary CTA to /explore, and embeds the demo.</done>
</task>

<task type="auto">
  <name>Task 2: Build ValuePropsGrid, PricingTeaser, and home page</name>
  <read_first>
    - .planning/phases/01-marketing-surface/01-UI-SPEC.md (value props copy table, pricing teaser copy table, home page layout spec)
    - .planning/phases/01-marketing-surface/01-PATTERNS.md (page.tsx pattern, value-props-grid no-analog note, pricing-teaser no-analog note)
  </read_first>
  <files>
    src/components/marketing/value-props-grid.tsx,
    src/components/marketing/pricing-teaser.tsx,
    src/app/(marketing)/page.tsx
  </files>
  <action>
**Create src/components/marketing/value-props-grid.tsx** — Server Component (NO `'use client'`). Pure Tailwind CSS grid, no shadcn primitive.

Exact copy verbatim from UI-SPEC copywriting contract:

| Slot | Heading | Body |
|---|---|---|
| 1 | `Lima format, satu sumber` | `Framer, Webflow, HTML, JSX, TSX. Pilih sesuai stack-mu. Kode-nya konsisten di semua format.` |
| 2 | `Bayar pakai Rupiah` | `QRIS, e-wallet, transfer bank. Tanpa kartu kredit internasional. Tanpa konversi mata uang.` |
| 3 | `Dirancang buat copy-paste` | `Setiap komponen sudah teruji. Tinggal salin, tempel, dan sesuaikan. Nggak ada vendor lock-in.` |

```tsx
// Server Component — no 'use client'
export function ValuePropsGrid() {
  const props = [
    {
      heading: "Lima format, satu sumber",
      body: "Framer, Webflow, HTML, JSX, TSX. Pilih sesuai stack-mu. Kode-nya konsisten di semua format.",
    },
    {
      heading: "Bayar pakai Rupiah",
      body: "QRIS, e-wallet, transfer bank. Tanpa kartu kredit internasional. Tanpa konversi mata uang.",
    },
    {
      heading: "Dirancang buat copy-paste",
      body: "Setiap komponen sudah teruji. Tinggal salin, tempel, dan sesuaikan. Nggak ada vendor lock-in.",
    },
  ];

  return (
    <section className="px-4 sm:px-8 lg:px-12 py-24 lg:py-32">
      <div className="max-w-[1200px] mx-auto">
        <div className="grid grid-cols-1 gap-8 lg:grid-cols-3 lg:gap-8">
          {props.map((prop) => (
            <div key={prop.heading} className="p-6">
              <h3 className="text-base font-semibold leading-[1.5] text-neutral-950 mb-3">
                {prop.heading}
              </h3>
              <p className="text-base font-normal leading-[1.5] text-neutral-950">
                {prop.body}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

**Create src/components/marketing/pricing-teaser.tsx** — Server Component (NO `'use client'`). Centered, max-w-[640px].

Exact copy verbatim from UI-SPEC:
- Heading: `Satu kali bayar, atau langganan`
- Body: `Pilih paket yang cocok buat ritme kerjamu. Cancel kapan saja di paket bulanan.`
- CTA link: `Lihat semua paket →` → `/pricing`

```tsx
import Link from "next/link";

export function PricingTeaser() {
  return (
    <section className="px-4 sm:px-8 lg:px-12 py-24 lg:py-32">
      <div className="max-w-[640px] mx-auto text-center">
        <h2 className="text-[1.75rem] lg:text-[2.5rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950">
          Satu kali bayar, atau langganan
        </h2>
        <p className="mt-4 text-base font-normal leading-[1.5] text-neutral-950">
          Pilih paket yang cocok buat ritme kerjamu. Cancel kapan saja di paket bulanan.
        </p>
        <Link
          href="/pricing"
          className="mt-6 inline-block text-base font-semibold text-neutral-950 underline underline-offset-4 hover:text-neutral-700 transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950 rounded"
        >
          Lihat semua paket →
        </Link>
      </div>
    </section>
  );
}
```

**Create src/app/(marketing)/page.tsx** — Server Component. Composes the three sections and exports page metadata.

```tsx
import type { Metadata } from "next";
import { HeroSection } from "@/components/marketing/hero-section";
import { ValuePropsGrid } from "@/components/marketing/value-props-grid";
import { PricingTeaser } from "@/components/marketing/pricing-teaser";

export const metadata: Metadata = {
  title: "lailit.supply — Komponen kreatif untuk developer Indonesia",
  description: "Animasi siap pakai, lima format kode, satu langganan. Buat developer yang nggak mau bikin ulang interaksi yang sama tiap project.",
};

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <ValuePropsGrid />
      <PricingTeaser />
    </>
  );
}
```

**Also:** If a placeholder `page.tsx` was created during Plan 01 build verification, replace it with this file now.
  </action>
  <verify>
    <automated>grep -n "Lima format, satu sumber" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/value-props-grid.tsx && grep -n "Bayar pakai Rupiah" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/value-props-grid.tsx && grep -n "Dirancang buat copy-paste" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/value-props-grid.tsx && grep -n "Satu kali bayar" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/pricing-teaser.tsx && grep -n "Lihat semua paket" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/pricing-teaser.tsx && grep -n "HeroSection" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/page.tsx && grep -n "ValuePropsGrid" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/page.tsx && grep -n "PricingTeaser" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "use client" src/components/marketing/value-props-grid.tsx` returns ZERO lines
    - `grep "use client" src/components/marketing/pricing-teaser.tsx` returns ZERO lines
    - `grep "Lima format, satu sumber" src/components/marketing/value-props-grid.tsx` returns a match (verbatim)
    - `grep "Bayar pakai Rupiah" src/components/marketing/value-props-grid.tsx` returns a match (verbatim)
    - `grep "Dirancang buat copy-paste" src/components/marketing/value-props-grid.tsx` returns a match (verbatim)
    - `grep "nggak ada vendor lock-in" src/components/marketing/value-props-grid.tsx` returns a match (lowercase informal tone per D-09)
    - `grep "Satu kali bayar, atau langganan" src/components/marketing/pricing-teaser.tsx` returns a match (verbatim)
    - `grep "Lihat semua paket" src/components/marketing/pricing-teaser.tsx` returns a match
    - `grep 'href="/pricing"' src/components/marketing/pricing-teaser.tsx` returns a match
    - `grep "grid-cols-3" src/components/marketing/value-props-grid.tsx` returns a match (desktop 3-col)
    - `grep "grid-cols-1" src/components/marketing/value-props-grid.tsx` returns a match (mobile 1-col)
    - `src/app/(marketing)/page.tsx` imports and renders HeroSection, ValuePropsGrid, PricingTeaser
    - `npm run build` completes with exit code 0
  </acceptance_criteria>
  <done>Home page renders: display headline, sub-headline, two CTAs, CSS-only animated demo, 3-column value props grid (1-col on mobile), and pricing teaser with link to /pricing. All copy is Bahasa Indonesia verbatim from UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → static HTML | Home page is a pure static Server Component. No user input, no API calls, no dynamic data. Zero server-side trust boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | Secondary CTA `/explore` (stubbed) | accept | Link navigates to /explore which 404s in Phase 1. No sensitive content. Acceptable stub. Wired in Phase 3. |
| T-01-02-02 | Denial of Service | CSS animation loop (orbit-cursor, button-pulse) | mitigate | Infinite CSS animation disabled under `prefers-reduced-motion: reduce` (rule in globals.css from Plan 01). No requestAnimationFrame or JS timers involved. |
| T-01-02-03 | Tampering | Open Graph metadata title/description | accept | Static metadata only. No user input influences metadata. No dynamic XSS vector. |
</threat_model>

<verification>
```bash
cd /Users/calvinlai/Desktop/lailit.supply && npm run build 2>&1 | tail -20
```

Expected: Build succeeds. Route `/` appears in the static output.

Spot-check copy verbatim strings are present:
```bash
grep "nggak mau bikin ulang" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/hero-section.tsx
grep "QRIS, e-wallet" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/value-props-grid.tsx
grep "Cancel kapan saja" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/pricing-teaser.tsx
```
</verification>

<success_criteria>
- Home page composes HeroSection, ValuePropsGrid, PricingTeaser as Server Components
- Display headline: "Komponen kreatif untuk developer Indonesia" — `text-[3rem] lg:text-[6rem] font-semibold leading-[1.05] tracking-[-0.03em]`
- Sub-headline: "Animasi siap pakai, lima format kode, satu langganan…" (verbatim)
- Primary CTA "Mulai Sekarang" links to /pricing; secondary "Lihat Contoh" links to /explore
- HeroAnimatedDemo: Server Component, CSS classes `hero-demo-cursor` and `hero-demo-button`, `role="img"`, `aria-label="Demo animasi tombol magnetic hover"`, `aspect-video` container
- ValuePropsGrid: 3 cells, `grid-cols-1 lg:grid-cols-3`, copy verbatim
- PricingTeaser: centered, max-w-[640px], "Satu kali bayar, atau langganan" heading, "Lihat semua paket →" link to /pricing
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-marketing-surface/01-02-SUMMARY.md` using the summary template.
</output>
