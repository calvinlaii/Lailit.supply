---
phase: 01-marketing-surface
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(marketing)/login/page.tsx
  - src/components/marketing/login-form.tsx
  - src/app/(marketing)/legal/privacy-policy/page.tsx
  - src/app/(marketing)/legal/terms-and-conditions/page.tsx
  - src/components/marketing/legal-page-layout.tsx
autonomous: true
requirements:
  - MKT-04
  - MKT-05
  - MKT-06

must_haves:
  truths:
    - "Visitor reaches /login and sees 'Masuk ke lailit.supply' heading, email input, and 'Kirim Magic Link' button"
    - "Submitting the login form with valid email does nothing (no-op, per D-15) — form submission calls e.preventDefault()"
    - "Submitting the login form with empty field shows 'Masukkan email kamu dulu.' in red below the input"
    - "Submitting the login form with invalid email format shows 'Format email belum benar. Coba cek lagi.' in red"
    - "Visitor can navigate to /legal/privacy-policy and read 'Kebijakan Privasi' with a breadcrumb back to home"
    - "Visitor can navigate to /legal/terms-and-conditions and read 'Syarat & Ketentuan' with a breadcrumb back to home"
    - "Legal pages render structured placeholder prose with [TODO: legal review] markers"
  artifacts:
    - path: "src/app/(marketing)/login/page.tsx"
      provides: "Login page centering the LoginForm card"
    - path: "src/components/marketing/login-form.tsx"
      provides: "Email input + magic-link CTA + client-side validation (use client)"
    - path: "src/components/marketing/legal-page-layout.tsx"
      provides: "Legal page wrapper: max-w-[720px], breadcrumb slot, prose article"
    - path: "src/app/(marketing)/legal/privacy-policy/page.tsx"
      provides: "Privacy policy page with placeholder prose"
    - path: "src/app/(marketing)/legal/terms-and-conditions/page.tsx"
      provides: "Terms and conditions page with placeholder prose"
  key_links:
    - from: "src/app/(marketing)/login/page.tsx"
      to: "src/components/marketing/login-form.tsx"
      via: "import + JSX render"
      pattern: "LoginForm"
    - from: "src/components/marketing/login-form.tsx"
      to: "src/components/ui/input.tsx"
      via: "import Input"
      pattern: "Input"
    - from: "src/components/marketing/login-form.tsx"
      to: "src/components/ui/label.tsx"
      via: "import Label"
      pattern: "Label"
    - from: "src/app/(marketing)/legal/privacy-policy/page.tsx"
      to: "src/components/marketing/legal-page-layout.tsx"
      via: "import + JSX render as wrapper"
      pattern: "LegalPageLayout"
---

<objective>
Build the login page with client-side validated magic-link form, and the two legal pages with prose layout.

Purpose: MKT-06 (login page), MKT-04 (legal pages). The login form is a no-op in Phase 2 but provides full client-side validation UX. Legal pages are static prose with placeholder content pending real legal copy pre-launch.

Output: /login page with validated no-op form; /legal/privacy-policy and /legal/terms-and-conditions with structured placeholder prose and breadcrumb navigation; shared LegalPageLayout component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-marketing-surface/01-UI-SPEC.md
@.planning/phases/01-marketing-surface/01-PATTERNS.md
@.planning/phases/01-marketing-surface/01-01-SUMMARY.md

<interfaces>
<!-- shadcn primitives installed by Plan 01 -->

shadcn Input API:
```tsx
import { Input } from "@/components/ui/input";
// <Input type="email" id="email" placeholder="kamu@email.com" className="h-11 ..." />
// h-11 = 44px (UI-SPEC login form spec: h-11 full width)
```

shadcn Label API:
```tsx
import { Label } from "@/components/ui/label";
// <Label htmlFor="email">Email</Label>
// Requires htmlFor to match input id for accessible association
```

Login form interaction states (UI-SPEC):
```
Email input resting:  border-neutral-200, h-11, px-4 py-3 rounded-lg, placeholder text-neutral-500
Email input hover:    hover:border-neutral-300
Email input focus:    focus:border-neutral-950 focus:outline-2 focus:outline-neutral-950/15 focus:outline-offset-1
Email input error:    border-red-600; error message text-red-600 text-sm below field
```

Login form copy (UI-SPEC):
```
Heading (h1):         "Masuk ke lailit.supply"
Sub-heading (muted):  "Kami akan kirim link masuk ke emailmu."
Email label:          "Email"
Placeholder:          "kamu@email.com"
CTA button:           "Kirim Magic Link"
Helper text:          "Belum punya akun? Magic link akan otomatis bikin akun baru."
Error — empty:        "Masukkan email kamu dulu."
Error — invalid:      "Format email belum benar. Coba cek lagi."
```

Legal pages copy (UI-SPEC):
```
Privacy heading (h1): "Kebijakan Privasi"
Privacy meta:         "Terakhir diperbarui: 5 Mei 2026"
Terms heading (h1):   "Syarat & Ketentuan"
Terms meta:           "Terakhir diperbarui: 5 Mei 2026"
Breadcrumb:           "← Kembali ke beranda" → "/"
```

Legal prose structure (UI-SPEC):
```
Privacy: sections — Pengumpulan Data / Penggunaan Data / Cookie / Kontak
Terms:   sections — Pemakaian Layanan / Pembayaran / Pembatalan / Lisensi / Kontak
Each section: <h2> + 2-3 <p> placeholder paragraphs + [TODO: legal review] marker
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build LoginForm component and login page</name>
  <read_first>
    - .planning/phases/01-marketing-surface/01-UI-SPEC.md (Login page section — all copy, all interaction states, form layout spec)
    - .planning/phases/01-marketing-surface/01-PATTERNS.md (login-form.tsx pattern, login/page.tsx pattern)
    - src/components/ui/input.tsx (read actual shadcn Input API and class names)
    - src/components/ui/label.tsx (read actual shadcn Label API)
  </read_first>
  <files>
    src/components/marketing/login-form.tsx,
    src/app/(marketing)/login/page.tsx
  </files>
  <behavior>
    - Empty email submit → error state: `errorMessage` = "Masukkan email kamu dulu." shown in `text-red-600 text-sm` below input, input border becomes `border-red-600`
    - Invalid email format (e.g. "notanemail") submit → error state: `errorMessage` = "Format email belum benar. Coba cek lagi." shown in `text-red-600 text-sm` below input
    - Valid email format (e.g. "user@example.com") submit → no error, form stays visible (no-op, `e.preventDefault()` only)
    - Error message is linked to input via `aria-describedby` (accessibility)
  </behavior>
  <action>
**Create src/components/marketing/login-form.tsx** — MUST have `'use client'` (uses useState for validation state + form onSubmit handler).

Full implementation:

```tsx
"use client";

import { useState } from "react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

function isValidEmail(email: string): boolean {
  // RFC 5322 simplified: must have local@domain.tld pattern
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim());
}

export function LoginForm() {
  const [email, setEmail] = useState("");
  const [errorMessage, setErrorMessage] = useState<string | null>(null);

  function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault(); // No-op — wired in Phase 2

    // Client-side validation
    if (!email.trim()) {
      setErrorMessage("Masukkan email kamu dulu.");
      return;
    }
    if (!isValidEmail(email)) {
      setErrorMessage("Format email belum benar. Coba cek lagi.");
      return;
    }

    // Valid email — clear error (form does nothing in Phase 1)
    setErrorMessage(null);
  }

  return (
    <div className="w-full max-w-[420px] bg-neutral-50 border border-neutral-200 rounded-[12px] p-8 shadow-[0_1px_2px_rgba(10,10,10,0.04),0_4px_12px_rgba(10,10,10,0.04)]">
      <h1 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 text-center">
        Masuk ke lailit.supply
      </h1>
      <p className="mt-2 text-base font-normal leading-[1.5] text-neutral-500 text-center">
        Kami akan kirim link masuk ke emailmu.
      </p>

      <form onSubmit={handleSubmit} noValidate className="mt-8">
        <div className="flex flex-col gap-2">
          <Label
            htmlFor="email"
            className="text-sm font-normal leading-[1.45] text-neutral-950"
          >
            Email
          </Label>
          <Input
            id="email"
            type="email"
            name="email"
            value={email}
            onChange={(e) => {
              setEmail(e.target.value);
              if (errorMessage) setErrorMessage(null); // clear error on change
            }}
            placeholder="kamu@email.com"
            autoComplete="email"
            aria-describedby={errorMessage ? "email-error" : undefined}
            aria-invalid={!!errorMessage}
            className={`h-11 text-base ${
              errorMessage
                ? "border-red-600 focus-visible:ring-red-600/15"
                : "border-neutral-200 hover:border-neutral-300 focus-visible:border-neutral-950"
            }`}
          />
          {errorMessage && (
            <p
              id="email-error"
              role="alert"
              className="text-sm font-normal leading-[1.45] text-red-600"
            >
              {errorMessage}
            </p>
          )}
        </div>

        <button
          type="submit"
          className="mt-4 w-full inline-flex items-center justify-center bg-neutral-950 text-white px-6 py-3 rounded-lg text-base font-semibold hover:bg-neutral-800 active:bg-neutral-900 transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950"
        >
          Kirim Magic Link
        </button>

        <p className="mt-3 text-sm font-normal leading-[1.45] text-neutral-500 text-center">
          Belum punya akun? Magic link akan otomatis bikin akun baru.
        </p>
      </form>
    </div>
  );
}
```

**Create src/app/(marketing)/login/page.tsx** — Server Component (NO `'use client'`). Centers the LoginForm card.

```tsx
import type { Metadata } from "next";
import { LoginForm } from "@/components/marketing/login-form";

export const metadata: Metadata = {
  title: "Masuk — lailit.supply",
  description: "Masuk ke lailit.supply. Kami akan kirim magic link ke emailmu.",
};

export default function LoginPage() {
  return (
    <div className="flex items-center justify-center min-h-[60vh] px-4 py-20">
      <LoginForm />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>grep -n "'use client'" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "Masuk ke lailit.supply" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "Kirim Magic Link" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "Masukkan email kamu dulu" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "Format email belum benar" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "aria-describedby" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "e.preventDefault" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx && grep -n "LoginForm" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/login/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/marketing/login-form.tsx` returns a match on line 1 (must be client component)
    - `grep "use client" src/app/(marketing)/login/page.tsx` returns ZERO lines (page is Server Component)
    - `grep "Masuk ke lailit.supply" src/components/marketing/login-form.tsx` returns a match (verbatim heading)
    - `grep "Kami akan kirim link masuk ke emailmu" src/components/marketing/login-form.tsx` returns a match (verbatim sub-heading)
    - `grep "kamu@email.com" src/components/marketing/login-form.tsx` returns a match (verbatim placeholder)
    - `grep "Kirim Magic Link" src/components/marketing/login-form.tsx` returns a match (verbatim CTA)
    - `grep "Masukkan email kamu dulu." src/components/marketing/login-form.tsx` returns a match (verbatim empty error)
    - `grep "Format email belum benar. Coba cek lagi." src/components/marketing/login-form.tsx` returns a match (verbatim invalid error)
    - `grep "Belum punya akun" src/components/marketing/login-form.tsx` returns a match (verbatim helper text)
    - `grep "e.preventDefault" src/components/marketing/login-form.tsx` returns a match (no-op form)
    - `grep 'aria-describedby' src/components/marketing/login-form.tsx` returns a match (accessibility error association)
    - `grep 'role="alert"' src/components/marketing/login-form.tsx` returns a match (accessible error announcement)
    - `grep "text-red-600" src/components/marketing/login-form.tsx` returns a match (error text color)
    - `grep "border-red-600" src/components/marketing/login-form.tsx` returns a match (error input border)
  </acceptance_criteria>
  <done>Login form with 'use client' handles empty-email and invalid-email validation showing verbatim Indonesian error copy in red-600; valid email submit is a no-op; all copy is verbatim from UI-SPEC; aria-describedby links error to input.</done>
</task>

<task type="auto">
  <name>Task 2: Build LegalPageLayout and legal pages</name>
  <read_first>
    - .planning/phases/01-marketing-surface/01-UI-SPEC.md (Legal pages section — headings, breadcrumb, prose structure, section names for privacy and terms)
    - .planning/phases/01-marketing-surface/01-PATTERNS.md (legal-page-layout.tsx pattern, legal page.tsx patterns)
  </read_first>
  <files>
    src/components/marketing/legal-page-layout.tsx,
    src/app/(marketing)/legal/privacy-policy/page.tsx,
    src/app/(marketing)/legal/terms-and-conditions/page.tsx
  </files>
  <action>
**Create src/components/marketing/legal-page-layout.tsx** — Server Component (NO `'use client'`). Wrapper that constrains content to 720px for readable prose line measure.

```tsx
import type { ReactNode } from "react";

type LegalPageLayoutProps = {
  children: ReactNode;
};

export function LegalPageLayout({ children }: LegalPageLayoutProps) {
  return (
    <div className="px-4 sm:px-8 lg:px-12 py-12 lg:py-16">
      <div className="max-w-[720px] mx-auto">
        {children}
      </div>
    </div>
  );
}
```

**Create src/app/(marketing)/legal/privacy-policy/page.tsx** — Server Component. Creates directories `src/app/(marketing)/legal/privacy-policy/` as needed.

Privacy policy structure per UI-SPEC:
- Breadcrumb: `← Kembali ke beranda` → `/`
- H1: `Kebijakan Privasi`
- Sub-heading meta: `Terakhir diperbarui: 5 Mei 2026`
- Prose sections with [TODO: legal review] markers: Pengumpulan Data / Penggunaan Data / Cookie / Kontak

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { LegalPageLayout } from "@/components/marketing/legal-page-layout";

export const metadata: Metadata = {
  title: "Kebijakan Privasi — lailit.supply",
};

export default function PrivacyPolicyPage() {
  return (
    <LegalPageLayout>
      <Link
        href="/"
        className="inline-block text-sm font-normal leading-[1.45] text-neutral-500 hover:text-neutral-950 transition-colors duration-150 mb-8 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950 rounded"
      >
        ← Kembali ke beranda
      </Link>

      <h1 className="text-[1.75rem] lg:text-[2.5rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950">
        Kebijakan Privasi
      </h1>
      <p className="mt-2 text-sm font-normal leading-[1.45] text-neutral-500">
        Terakhir diperbarui: 5 Mei 2026
      </p>

      <article className="mt-8 space-y-8">
        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Pengumpulan Data
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Kami mengumpulkan alamat email yang kamu berikan saat mendaftar atau login. Data ini digunakan untuk mengirim magic link autentikasi dan informasi akun. {/* [TODO: legal review] */}
          </p>
          <p className="mt-3 text-base font-normal leading-[1.5] text-neutral-950">
            Informasi pembayaran diproses sepenuhnya oleh Mayar.id dan tidak disimpan di server kami. Kami hanya menerima konfirmasi status pembayaran melalui webhook. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Penggunaan Data
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Data yang kami kumpulkan digunakan untuk: memberikan akses ke layanan lailit.supply, mengirim notifikasi penting terkait akun dan langganan kamu, dan meningkatkan layanan kami. {/* [TODO: legal review] */}
          </p>
          <p className="mt-3 text-base font-normal leading-[1.5] text-neutral-950">
            Kami tidak menjual atau berbagi data pribadi kamu dengan pihak ketiga untuk tujuan pemasaran. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Cookie
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            lailit.supply menggunakan cookie sesi untuk menjaga status login kamu. Cookie ini diperlukan untuk fungsi dasar layanan dan tidak dapat dinonaktifkan jika kamu ingin menggunakan layanan. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Kontak
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Untuk pertanyaan terkait privasi data kamu, hubungi kami di: <a href="mailto:hello@lailit.supply" className="underline underline-offset-4 hover:text-neutral-700">hello@lailit.supply</a>. {/* [TODO: legal review] */}
          </p>
        </section>
      </article>
    </LegalPageLayout>
  );
}
```

**Create src/app/(marketing)/legal/terms-and-conditions/page.tsx** — Server Component. Creates directories as needed.

Terms structure per UI-SPEC:
- Breadcrumb: `← Kembali ke beranda` → `/`
- H1: `Syarat & Ketentuan`
- Sub-heading meta: `Terakhir diperbarui: 5 Mei 2026`
- Prose sections with [TODO: legal review] markers: Pemakaian Layanan / Pembayaran / Pembatalan / Lisensi / Kontak

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { LegalPageLayout } from "@/components/marketing/legal-page-layout";

export const metadata: Metadata = {
  title: "Syarat & Ketentuan — lailit.supply",
};

export default function TermsAndConditionsPage() {
  return (
    <LegalPageLayout>
      <Link
        href="/"
        className="inline-block text-sm font-normal leading-[1.45] text-neutral-500 hover:text-neutral-950 transition-colors duration-150 mb-8 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950 rounded"
      >
        ← Kembali ke beranda
      </Link>

      <h1 className="text-[1.75rem] lg:text-[2.5rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950">
        Syarat &amp; Ketentuan
      </h1>
      <p className="mt-2 text-sm font-normal leading-[1.45] text-neutral-500">
        Terakhir diperbarui: 5 Mei 2026
      </p>

      <article className="mt-8 space-y-8">
        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Pemakaian Layanan
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Dengan menggunakan lailit.supply, kamu menyetujui syarat dan ketentuan ini. Layanan ini ditujukan untuk developer dan desainer yang ingin menggunakan komponen kreatif dalam proyek mereka. {/* [TODO: legal review] */}
          </p>
          <p className="mt-3 text-base font-normal leading-[1.5] text-neutral-950">
            Kamu tidak boleh menggunakan layanan ini untuk mendistribusikan ulang, menjual kembali, atau mengklaim komponen sebagai karyamu sendiri kepada pihak lain. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Pembayaran
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Semua pembayaran diproses melalui Mayar.id. Harga yang tertera sudah termasuk PPN. Kami menerima QRIS, e-wallet, dan transfer bank melalui platform Mayar.id. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Pembatalan
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Langganan bulanan dapat dibatalkan kapan saja. Akses tetap aktif hingga akhir periode langganan yang sudah dibayar. Tidak ada pengembalian dana untuk periode yang sudah berjalan. {/* [TODO: legal review] */}
          </p>
          <p className="mt-3 text-base font-normal leading-[1.5] text-neutral-950">
            Pembelian lifetime tidak dapat dikembalikan kecuali dalam kondisi yang diwajibkan oleh hukum yang berlaku. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Lisensi
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Kamu mendapatkan lisensi komersial untuk menggunakan komponen dalam proyek klien dan proyek pribadi. Lisensi ini non-eksklusif dan tidak dapat dipindahtangankan. {/* [TODO: legal review] */}
          </p>
        </section>

        <section>
          <h2 className="text-[1.75rem] font-semibold leading-[1.15] tracking-[-0.02em] text-neutral-950 mb-4">
            Kontak
          </h2>
          <p className="text-base font-normal leading-[1.5] text-neutral-950">
            Untuk pertanyaan terkait syarat dan ketentuan ini, hubungi kami di: <a href="mailto:hello@lailit.supply" className="underline underline-offset-4 hover:text-neutral-700">hello@lailit.supply</a>. {/* [TODO: legal review] */}
          </p>
        </section>
      </article>
    </LegalPageLayout>
  );
}
```
  </action>
  <verify>
    <automated>grep -n "Kebijakan Privasi" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/privacy-policy/page.tsx && grep -n "Syarat" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/terms-and-conditions/page.tsx && grep -n "Kembali ke beranda" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/privacy-policy/page.tsx && grep -n "TODO: legal review" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/privacy-policy/page.tsx && grep -n "max-w-\[720px\]" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/legal-page-layout.tsx && grep -n "Pengumpulan Data" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/privacy-policy/page.tsx && grep -n "Pemakaian Layanan" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/terms-and-conditions/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "use client" src/components/marketing/legal-page-layout.tsx` returns ZERO lines (Server Component)
    - `grep "max-w-\\[720px\\]" src/components/marketing/legal-page-layout.tsx` returns a match (prose width constraint)
    - `grep "Kebijakan Privasi" src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match (verbatim heading)
    - `grep "Terakhir diperbarui: 5 Mei 2026" src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match
    - `grep "Kembali ke beranda" src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match (breadcrumb)
    - `grep 'href="/"' src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match (breadcrumb links to /)
    - `grep "TODO: legal review" src/app/(marketing)/legal/privacy-policy/page.tsx` returns matches (placeholder markers)
    - `grep "Pengumpulan Data" src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match (privacy section)
    - `grep "Penggunaan Data" src/app/(marketing)/legal/privacy-policy/page.tsx` returns a match
    - `grep "Syarat" src/app/(marketing)/legal/terms-and-conditions/page.tsx` returns a match (verbatim heading)
    - `grep "Pemakaian Layanan" src/app/(marketing)/legal/terms-and-conditions/page.tsx` returns a match
    - `grep "Pembatalan" src/app/(marketing)/legal/terms-and-conditions/page.tsx` returns a match
    - `grep "Lisensi" src/app/(marketing)/legal/terms-and-conditions/page.tsx` returns a match
    - `npm run build` exits 0 (routes /legal/privacy-policy and /legal/terms-and-conditions appear)
  </acceptance_criteria>
  <done>LegalPageLayout constrains prose to max-w-[720px]; both legal pages have breadcrumb back to home, correct H1 headings, "Terakhir diperbarui: 5 Mei 2026" sub-heading, and structured placeholder prose with [TODO: legal review] markers in required section structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → login form (client-side only) | LoginForm validates email format in the browser. No data leaves the client in Phase 1 — form submission is `e.preventDefault()` only. The actual server submission is Phase 2. |
| Browser → static legal pages | Fully static Server Components. No user input, no API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Login form — email field | accept | Email is entered client-side only. No network request is made in Phase 1. No data stored. No server exposure. |
| T-01-04-02 | Spoofing | Login form — no-op submit | accept | Form does nothing. There is no authentication in Phase 1. The risk lands entirely in Phase 2 when `supabase.auth.signInWithOtp` is wired. Phase 2 plan will address session spoofing. |
| T-01-04-03 | Tampering | Legal page placeholder content | accept | Placeholder content is explicitly marked `[TODO: legal review]`. It does not contain enforceable legal terms. Real copy will replace this pre-launch via native-speaker review (noted in STATE.md). |
| T-01-04-04 | Denial of Service | Login form validation error on every keystroke | mitigate | Error clears `onChange` to avoid persistent red state while typing. `noValidate` prevents browser validation conflicts. Pattern is reactive not eager — only validates on submit. |
</threat_model>

<verification>
```bash
cd /Users/calvinlai/Desktop/lailit.supply && npm run build 2>&1 | tail -30
```

Expected: Build succeeds. Routes `/login`, `/legal/privacy-policy`, `/legal/terms-and-conditions` appear in static output.

Spot-check key copy strings:
```bash
grep "Masukkan email kamu dulu." /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx
grep "Format email belum benar." /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/login-form.tsx
grep "TODO: legal review" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/legal/privacy-policy/page.tsx | wc -l
```

Third command should return ≥ 4 (one per section).
</verification>

<success_criteria>
- /login page: LoginForm card centered in 60vh min-height; H1 "Masuk ke lailit.supply"; sub-heading; email input with Label; "Kirim Magic Link" button; helper text "Belum punya akun? Magic link akan otomatis bikin akun baru."
- Login form validation: empty → "Masukkan email kamu dulu." in red-600; invalid format → "Format email belum benar. Coba cek lagi." in red-600; error linked via aria-describedby; valid email → no-op
- /legal/privacy-policy: H1 "Kebijakan Privasi"; meta date; breadcrumb; 4 sections (Pengumpulan Data, Penggunaan Data, Cookie, Kontak); [TODO: legal review] markers
- /legal/terms-and-conditions: H1 "Syarat & Ketentuan"; meta date; breadcrumb; 5 sections (Pemakaian Layanan, Pembayaran, Pembatalan, Lisensi, Kontak); [TODO: legal review] markers
- LegalPageLayout: max-w-[720px] prose constraint
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-marketing-surface/01-04-SUMMARY.md` using the summary template.
</output>
