---
phase: 01-marketing-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/(marketing)/layout.tsx
  - src/components/marketing/top-nav.tsx
  - src/components/marketing/footer.tsx
autonomous: true
requirements:
  - MKT-05

must_haves:
  truths:
    - "shadcn/ui v3 (new-york / neutral) is initialized with all Phase 1 primitives installed"
    - "Dark-mode block is removed from globals.css and Geist Sans is wired as body font"
    - "Root layout serves <html lang='id'> with correct metadata"
    - "Marketing route group layout wraps pages in <TopNav /> + <main id='main'> + <Footer />"
    - "TopNav renders logo, three nav links, and a mobile Sheet drawer on viewports < lg"
    - "Footer renders 3-column link grid, tagline, and copyright line"
    - "Skip link is the first focusable element on every marketing page"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button primitive"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet primitive (mobile nav)"
    - path: "src/lib/utils.ts"
      provides: "cn() utility from shadcn init"
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Marketing route-group layout shell"
    - path: "src/components/marketing/top-nav.tsx"
      provides: "Sticky navigation with mobile drawer"
    - path: "src/components/marketing/footer.tsx"
      provides: "Site footer"
  key_links:
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/marketing/top-nav.tsx"
      via: "import + JSX render"
      pattern: "TopNav"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/marketing/footer.tsx"
      via: "import + JSX render"
      pattern: "Footer"
    - from: "src/components/marketing/top-nav.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "import Sheet, SheetContent, SheetTrigger"
      pattern: "SheetTrigger"
---

<objective>
Initialize the design system and build the reusable site shell for all marketing pages.

Purpose: This plan creates the foundation every other Phase 1 plan depends on — shadcn/ui primitives, design tokens, root layout fixes, marketing route-group layout, navigation, and footer. Nothing else in Phase 1 can run until this plan completes.

Output: shadcn initialized; globals.css with Phase 1 tokens; root layout with `lang="id"`; marketing layout shell; TopNav with mobile hamburger Sheet; Footer with 3-column link grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-marketing-surface/01-UI-SPEC.md
@.planning/phases/01-marketing-surface/01-PATTERNS.md
@.planning/research/STACK.md

<interfaces>
<!-- Current state of files this plan modifies. Executor reads these before touching anything. -->

From src/app/globals.css (current — DELETE lines 15–20 dark-mode block, fix body font):
```css
@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

/* DELETE THIS BLOCK — conflicts with D-01 */
@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;  /* CHANGE to var(--font-geist-sans) */
}
```

From src/app/layout.tsx (current — change lang="en" to lang="id", update metadata):
```tsx
// lang="en" on line 27 — must become lang="id"
// metadata title/description — must become lailit.supply brand copy
// body className="min-h-full flex flex-col" — keep as-is
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and fix globals.css + root layout</name>
  <read_first>
    - src/app/globals.css (read current state before modifying)
    - src/app/layout.tsx (read current state before modifying)
    - node_modules/next/dist/docs/01-app/01-getting-started/11-css.md (Tailwind v4 conventions)
  </read_first>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
**Step 1 — Initialize shadcn/ui v3:**
Run the following commands in sequence (non-interactive flags where possible). When `npx shadcn@latest init` prompts, select: style = `new-york`, baseColor = `neutral`, cssVariables = yes. This creates `src/components/ui/`, `src/lib/utils.ts`, and injects shadcn CSS variables into `globals.css`.

```bash
npx shadcn@latest init
npx shadcn@latest add button card badge input label form sheet
```

**Step 2 — Fix globals.css after shadcn init:**
After shadcn injects its CSS variable block, the file will have both the original content and shadcn additions. Apply these exact edits:

1. DELETE the `@media (prefers-color-scheme: dark) { :root { --background: #0a0a0a; --foreground: #ededed; } }` block entirely (conflicts with D-01 — light-first, no dark mode at MVP).

2. Replace `font-family: Arial, Helvetica, sans-serif` with `font-family: var(--font-geist-sans)` in the `body {}` rule (per D-03 — Geist font throughout).

3. ADD these blocks after the body rule:

```css
/* Phase 1 radius tokens */
:root {
  --radius: 12px;        /* cards */
  --radius-sm: 8px;      /* buttons, inputs */
  --radius-full: 9999px; /* "Paling Populer" pill badge */
}

/* Reduced motion: disable all transitions and animations site-wide */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    transition-duration: 0ms !important;
    animation-duration: 0ms !important;
    animation-iteration-count: 1 !important;
  }
}

/* Hero CSS animation keyframes (used by hero-animated-demo.tsx) */
@keyframes orbit-cursor {
  0%   { transform: translate(80px, 0); }
  25%  { transform: translate(0, -80px); }
  50%  { transform: translate(-80px, 0); }
  75%  { transform: translate(0, 80px); }
  100% { transform: translate(80px, 0); }
}

@keyframes button-pulse {
  0%, 100% { transform: scale(1) translateX(0); }
  25%       { transform: scale(1.04) translateX(4px); }
  75%       { transform: scale(1.04) translateX(-4px); }
}

.hero-demo-cursor {
  animation: orbit-cursor 4s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}

.hero-demo-button {
  animation: button-pulse 4s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}

@media (prefers-reduced-motion: reduce) {
  .hero-demo-cursor,
  .hero-demo-button {
    animation: none;
  }
}
```

**Step 3 — Fix src/app/layout.tsx:**
Make exactly these two changes:
1. Change `lang="en"` to `lang="id"` (line ~27)
2. Replace the metadata export:
```tsx
export const metadata: Metadata = {
  title: "lailit.supply — Komponen kreatif untuk developer Indonesia",
  description: "Animasi siap pakai, lima format kode, satu langganan.",
};
```

Keep the body className, Geist font wiring, and everything else unchanged.
  </action>
  <verify>
    <automated>grep -n 'lang="id"' /Users/calvinlai/Desktop/lailit.supply/src/app/layout.tsx && grep -n 'lailit.supply' /Users/calvinlai/Desktop/lailit.supply/src/app/layout.tsx && grep -n 'prefers-color-scheme' /Users/calvinlai/Desktop/lailit.supply/src/app/globals.css | wc -l && grep -n 'font-geist-sans' /Users/calvinlai/Desktop/lailit.supply/src/app/globals.css && ls /Users/calvinlai/Desktop/lailit.supply/src/components/ui/button.tsx && ls /Users/calvinlai/Desktop/lailit.supply/src/lib/utils.ts && ls /Users/calvinlai/Desktop/lailit.supply/src/components/ui/sheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'lang="id"' src/app/layout.tsx` returns a match
    - `grep 'prefers-color-scheme' src/app/globals.css` returns zero lines (dark block deleted)
    - `grep 'font-geist-sans' src/app/globals.css` returns a match on the body font-family line
    - `grep 'orbit-cursor' src/app/globals.css` returns a match (keyframes present)
    - `grep 'radius-full' src/app/globals.css` returns a match (radius tokens present)
    - `src/components/ui/button.tsx` exists (shadcn init succeeded)
    - `src/components/ui/sheet.tsx` exists
    - `src/lib/utils.ts` exists and contains `export function cn`
  </acceptance_criteria>
  <done>shadcn initialized with new-york/neutral style; globals.css has no dark-mode block, has Geist font body rule, has radius tokens, has hero keyframes, has reduced-motion rules; root layout has lang="id" and lailit.supply metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Build marketing layout shell with TopNav and Footer</name>
  <read_first>
    - src/app/layout.tsx (confirm body flex-col pattern before creating nested layout)
    - src/components/ui/sheet.tsx (read Sheet API before using SheetTrigger/SheetContent)
    - .planning/phases/01-marketing-surface/01-UI-SPEC.md (TopNav copy contract, Footer copy contract, mobile nav drawer spec, skip link pattern)
    - .planning/phases/01-marketing-surface/01-PATTERNS.md (top-nav.tsx pattern, footer.tsx pattern, marketing layout pattern)
  </read_first>
  <files>
    src/app/(marketing)/layout.tsx,
    src/components/marketing/top-nav.tsx,
    src/components/marketing/footer.tsx
  </files>
  <action>
**Create src/app/(marketing)/layout.tsx** — Server Component, no `'use client'`. This is a nested layout (no `<html>` or `<body>` tags). It wraps children with TopNav + main + Footer. The skip link is the FIRST focusable element. `flex-1` on `<main>` ensures footer sticks to bottom (root body is `flex flex-col`).

```tsx
import { TopNav } from "@/components/marketing/top-nav";
import { Footer } from "@/components/marketing/footer";

export default function MarketingLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <>
      <a
        href="#main"
        className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:bg-white focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-neutral-950 focus:border focus:border-neutral-200 focus:rounded-lg"
      >
        Lewati ke konten utama
      </a>
      <TopNav />
      <main id="main" className="flex-1">
        {children}
      </main>
      <Footer />
    </>
  );
}
```

**Create src/components/marketing/top-nav.tsx** — `'use client'` (uses useState for Sheet open state + usePathname for active link).

Exact implementation:
- Sticky nav: `sticky top-0 z-50 h-16 border-b border-neutral-200 bg-white`
- Inner container: `max-w-[1200px] mx-auto px-4 sm:px-8 lg:px-12 h-full flex items-center justify-between`
- Logo: `<Link href="/" aria-label="lailit.supply, kembali ke beranda">` with text `lailit.supply` — class: `text-base font-semibold text-neutral-950`
- Desktop nav links (hidden below lg, flex gap-8 on lg+): `hidden lg:flex items-center gap-8`
  - `Komponen` → `href="/explore"` (stubbed — page doesn't exist yet)
  - `Harga` → `href="/pricing"`
  - `Masuk` → `href="/login"`
  - Each link: `text-sm text-neutral-950 underline-offset-4 hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950 rounded` + active state: append `underline` when `pathname === href`
- Mobile hamburger (visible below lg, hidden on lg+): `lg:hidden`
  - `<Sheet open={isOpen} onOpenChange={setIsOpen}>`
  - `<SheetTrigger asChild>`
  - `<button className="flex h-11 w-11 items-center justify-center rounded-lg hover:bg-neutral-100 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-neutral-950" aria-label={isOpen ? "Tutup menu" : "Buka menu"} aria-expanded={isOpen}>`
  - Icon: `{isOpen ? <X size={24} /> : <Menu size={24} />}` (from lucide-react)
  - `<SheetContent side="right" className="w-[280px] p-6">` — contains 3 nav links stacked with `flex flex-col gap-8`
  - Each mobile link: `text-base text-neutral-950 py-3 min-h-[44px] flex items-center hover:underline underline-offset-4`
  - Close sheet on link click: `onClick={() => setIsOpen(false)}`
  - Backdrop: the Sheet primitive handles this automatically; add `className` of `bg-neutral-950/40` on SheetOverlay if shadcn exposes it

Imports needed:
```tsx
"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Menu, X } from "lucide-react";
import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet";
import { useState } from "react";
```

**Create src/components/marketing/footer.tsx** — Server Component (no `'use client'`).

Exact structure:
- Outer: `<footer className="border-t border-neutral-200 bg-neutral-50">`
- Inner container: `max-w-[1200px] mx-auto px-4 sm:px-8 lg:px-12 py-12 lg:py-16`
- Grid: `grid grid-cols-2 gap-8 lg:grid-cols-4`
- Column 0 (tagline, spans full width on mobile): Tagline `Komponen kreatif untuk developer Indonesia.` — `text-sm text-neutral-500`
- Column 1 heading `Produk` (label, 600 weight): links — `Komponen` → `/explore`, `Harga` → `/pricing`
- Column 2 heading `Akun`: links — `Masuk` → `/login`
- Column 3 heading `Legal`: links — `Kebijakan Privasi` → `/legal/privacy-policy`, `Syarat & Ketentuan` → `/legal/terms-and-conditions`
- Below grid (mt-12 border-t border-neutral-200 pt-8): `© 2026 lailit.supply. Dibuat di Indonesia.` — `text-sm text-neutral-500`
- All footer links: `text-sm text-neutral-500 hover:text-neutral-950 transition-colors`
- Column headings: `text-sm font-semibold text-neutral-950 mb-3`
  </action>
  <verify>
    <automated>ls /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/layout.tsx && grep -n "TopNav" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/layout.tsx && grep -n "Footer" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/layout.tsx && grep -n "Lewati ke konten utama" /Users/calvinlai/Desktop/lailit.supply/src/app/\(marketing\)/layout.tsx && grep -n "use client" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/top-nav.tsx && grep -n "SheetTrigger" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/top-nav.tsx && grep -n "Komponen kreatif" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/footer.tsx && grep -n "Dibuat di Indonesia" /Users/calvinlai/Desktop/lailit.supply/src/components/marketing/footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(marketing)/layout.tsx` exists and contains `id="main"` on the main element
    - `src/app/(marketing)/layout.tsx` contains `Lewati ke konten utama` (skip link)
    - `src/app/(marketing)/layout.tsx` does NOT contain `'use client'`
    - `src/components/marketing/top-nav.tsx` contains `'use client'` on first line
    - `grep "SheetTrigger" src/components/marketing/top-nav.tsx` returns a match
    - `grep "aria-label.*Buka menu" src/components/marketing/top-nav.tsx` returns a match
    - `grep "aria-label.*Tutup menu" src/components/marketing/top-nav.tsx` returns a match
    - `grep "lailit.supply" src/components/marketing/top-nav.tsx` returns a match (logo wordmark)
    - `src/components/marketing/footer.tsx` does NOT contain `'use client'`
    - `grep "Kebijakan Privasi" src/components/marketing/footer.tsx` returns a match
    - `grep "Syarat.*Ketentuan" src/components/marketing/footer.tsx` returns a match
    - `grep "2026 lailit.supply" src/components/marketing/footer.tsx` returns a match
  </acceptance_criteria>
  <done>Marketing layout shell exists at src/app/(marketing)/layout.tsx; TopNav renders on all marketing pages with sticky positioning, logo, 3 desktop nav links, and a Sheet-powered mobile hamburger; Footer renders 3-column link grid with copyright.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js static render | All Phase 1 pages are static Server Components — no user-supplied input reaches the server except through the stubbed login form (no-op). Zero server-side trust boundary violations possible in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | globals.css (dark-mode deletion) | accept | Dark-mode block deletion is intentional and irreversible within Phase 1. No security implication — cosmetic CSS only. |
| T-01-02 | Information Disclosure | TopNav stubbed `/explore` href | accept | `/explore` does not exist in Phase 1; Next.js will render a 404. No sensitive content reachable. Acceptable for stubbed route. |
| T-01-03 | Spoofing | Sheet mobile nav (no auth state) | accept | Nav in Phase 1 has no auth-gated links. No session tokens handled. No spoofing vector. |
| T-01-04 | Denial of Service | CSS animation (hero keyframes in globals.css) | mitigate | `prefers-reduced-motion: reduce` rule stops all animations. No JS bundle shipped. Negligible CPU impact from CSS-only animation. Rule added in Task 1. |
</threat_model>

<verification>
After plan execution, verify the full marketing shell renders:

```bash
cd /Users/calvinlai/Desktop/lailit.supply && npm run build 2>&1 | tail -20
```

Expected: Build succeeds. If it fails due to missing page.tsx in (marketing) group, create a temporary `src/app/(marketing)/page.tsx` that exports `export default function Page() { return null; }` and re-run. Remove after Plan 02 creates the real home page.

Also verify TypeScript:
```bash
cd /Users/calvinlai/Desktop/lailit.supply && npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
- shadcn/ui v3 initialized with new-york/neutral style
- `button`, `card`, `badge`, `input`, `label`, `form`, `sheet` primitives installed
- globals.css: no dark-mode media query block, `font-family: var(--font-geist-sans)` on body, radius tokens present, hero keyframes present, reduced-motion rules present
- src/app/layout.tsx: `lang="id"`, metadata title = "lailit.supply — Komponen kreatif untuk developer Indonesia"
- src/app/(marketing)/layout.tsx: skip link first, TopNav, main#main, Footer
- src/components/marketing/top-nav.tsx: sticky, logo, 3 desktop nav links, mobile Sheet hamburger with 44×44px touch target, aria-labels "Buka menu"/"Tutup menu"
- src/components/marketing/footer.tsx: 3-column links (Produk, Akun, Legal), tagline, copyright "© 2026 lailit.supply. Dibuat di Indonesia."
</success_criteria>

<output>
After completion, create `.planning/phases/01-marketing-surface/01-01-SUMMARY.md` using the summary template.
</output>
