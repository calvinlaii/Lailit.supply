---
phase: 03-content-pipeline
plan: 06
type: execute
wave: 4
depends_on: [03-05]
files_modified: []
autonomous: false
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - CONTENT-04
  - CONTENT-05
  - CONTENT-06
  - MKT-03
  - GATE-01

must_haves:
  truths:
    - "Unauthenticated browser visit to /explore shows 20 resource cards without login prompt"
    - "Category filter chips work client-side — clicking 'Animation' shows only animation resources, no page reload"
    - "Free resource /explore/{slug} renders format tabs and syntax-highlighted code block"
    - "Premium resource /explore/{slug} shows PaywallStub (lock icon + 'Konten Premium' + 'Lihat Paket Harga') with no code content"
    - "CopyButton copies code to clipboard and shows Check icon for 2 seconds"
    - "`npx next build` passes clean (exit 0, no build errors)"
    - "Adding a new resource folder to content/resources/ and running `next build` makes it appear in /explore"
  artifacts:
    - path: "src/app/(marketing)/explore/page.tsx"
      provides: "Verified live /explore page"
      contains: "getAllResources"
    - path: "src/app/(marketing)/explore/[slug]/page.tsx"
      provides: "Verified resource detail pages"
      exports: ["generateStaticParams"]
  key_links:
    - from: "/explore"
      to: "content/resources"
      via: "getAllResources() manifest at build/request time"
      pattern: "getAllResources"
---

<objective>
Human verification of the complete Phase 3 content pipeline. Verify /explore, resource detail pages (free and premium), category filtering, copy button, build success, and CONTENT-06 (new resource auto-discovery).

Purpose: Confirm the full user-facing experience works end-to-end before marking Phase 3 complete.
Output: No code changes — verification only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md §Phase 3 Success Criteria
@.planning/phases/03-content-pipeline/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build validation and automated checks</name>
  <read_first>
    - /Users/calvinlai/Desktop/lailit.supply/.planning/ROADMAP.md §Phase 3 Success Criteria
  </read_first>
  <files>(no files modified)</files>
  <action>
Run the following automated validation checks in order:

1. **Full build:**
   ```bash
   npx next build
   ```
   Must exit 0. If it fails, review build output for errors and fix the root cause before proceeding.

2. **Full test suite:**
   ```bash
   npx vitest run --reporter=verbose
   ```
   Must show 0 failures. All pre-existing tests (LoginForm) plus new content module tests must pass.

3. **TypeScript compile check:**
   ```bash
   npx tsc --noEmit
   ```
   Must show 0 errors.

4. **Content structure validation:**
   ```bash
   find content/resources -name "index.mdx" | wc -l
   ```
   Must output 20.
   
   ```bash
   grep -r "is_premium: false" content/resources/*/index.mdx | wc -l
   ```
   Must output 12.
   
   ```bash
   grep -r "is_premium: true" content/resources/*/index.mdx | wc -l
   ```
   Must output 8.

5. **Security check — no MDX in src/app:**
   ```bash
   find src/app -name "*.mdx" 2>/dev/null | wc -l
   ```
   Must output 0.

6. **CONTENT-06 verification (admin workflow):**
   - Create a new test resource folder: `content/resources/test-new-resource/`
   - Create `content/resources/test-new-resource/index.mdx` with valid frontmatter:
     ```mdx
     ---
     title: Test New Resource
     category: animation
     tags: [test]
     is_premium: false
     available_formats: [html]
     mux_playback_id: null
     demo_url: null
     ---
     ```
   - Create `content/resources/test-new-resource/html.mdx` with a code block
   - Run `npx next build` — the new resource must appear in `generateStaticParams` output
   - After verifying, DELETE the test folder (it was only for CONTENT-06 validation)

7. **Grep security check — premium content not rendered:**
   Verify that the slug for any premium resource (e.g., `animation-spring-bounce`) returns PaywallStub, not code content. Start the dev server and check:
   ```bash
   npx next dev &
   sleep 5
   curl -s http://localhost:3000/explore/animation-spring-bounce | grep -i "konten premium"
   ```
   Must return a match (PaywallStub is rendered). Then:
   ```bash
   curl -s http://localhost:3000/explore/animation-spring-bounce | grep -i "placeholder framer"
   ```
   Must return EMPTY (no premium code in response).

8. **Stop dev server after checks.**
  </action>
  <verify>
    <automated>cd /Users/calvinlai/Desktop/lailit.supply && npx next build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npx next build` exits 0
    - `npx vitest run` shows 0 failures
    - `npx tsc --noEmit` shows 0 errors
    - 20 index.mdx files confirmed in content/resources/
    - CONTENT-06 test resource appeared in build, then deleted
    - Premium resource curl returns "konten premium" match and NO code content
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 content pipeline:
- 20 seed resources across 4 categories (animation, ui-components, layout, interactions)
- /explore public catalog page with category filter chips (client-side, no reload)
- /explore/[slug] resource detail pages (free: format tabs + syntax-highlighted code + copy button; premium: paywall stub)
- Zod-validated build-time manifest (build fails loudly on invalid frontmatter)
- All pages statically pre-rendered via generateStaticParams
  </what-built>
  <how-to-verify>
Start the dev server: `npx next dev`

**Check 1 — /explore catalog (MKT-03, GATE-01):**
1. Open http://localhost:3000/explore in a browser (no login)
2. Verify: 20 resource cards visible in a responsive grid (1 col mobile, 2 col tablet, 3-4 col desktop)
3. Verify: Each card shows a gradient thumbnail, title, "Gratis" or "Premium" badge, and category label
4. Click "Animation" chip → only animation resources show (no page reload)
5. Click "UI Components" chip → only UI component resources show
6. Click "Semua" chip → all 20 resources show
7. Verify: No login prompt, no redirect to /login

**Check 2 — Free resource detail (CONTENT-01, CONTENT-02, GATE-01):**
1. Click any card with a "Gratis" badge (e.g., "Fade In On Scroll")
2. Verify: URL changes to /explore/animation-fade-in-on-scroll
3. Verify: "← Kembali ke Jelajahi" back link present
4. Verify: FormatTabBar shows only the formats listed for this resource
5. Verify: Code block renders with syntax highlighting (Shiki github-dark-dimmed theme)
6. Click the copy button → button icon changes to Check for ~2 seconds then reverts
7. Paste in a text editor → code should be pasted

**Check 3 — Premium resource detail (D-09, GATE-01):**
1. Click any card with a "Premium" badge (e.g., "Spring Bounce")
2. Verify: URL changes to /explore/animation-spring-bounce
3. Verify: "Konten Premium" heading is visible
4. Verify: "Lihat Paket Harga" button present, clicking it goes to /pricing
5. Verify: NO code content visible (no format tabs, no code block with real code)

**Check 4 — Build (CONTENT-03, CONTENT-06):**
1. Run `npx next build` in a terminal
2. Verify: Build exits 0 with no errors
3. Verify: Build output shows static generation for /explore and /explore/[slug] routes

**Check 5 — Invalid frontmatter (CONTENT-03):**
1. Open `content/resources/animation-fade-in-on-scroll/index.mdx`
2. Remove the `title:` line
3. Run `npx next build`
4. Verify: Build FAILS with an error message containing "animation-fade-in-on-scroll"
5. Restore the `title:` line and verify build passes again
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only — no new code) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | Phase 3 paywall stub completeness | mitigate | Verified by curl check in Task 1 step 7: premium resource HTML response must NOT contain placeholder code text |
</threat_model>

<verification>
Phase 3 is complete when:
1. `npx next build` exits 0
2. `npx vitest run` shows 0 failures
3. Human verifies all 5 checks in the checkpoint task
4. curl to a premium resource slug returns "konten premium" and NOT the placeholder code content
</verification>

<success_criteria>
All Phase 3 ROADMAP success criteria satisfied:
1. Admin adds new MDX folder → after build it appears in manifest (CONTENT-06 verified)
2. Build fails loudly on invalid frontmatter (CONTENT-03 verified)
3. Unauthenticated visitor browses /explore with 20 seed resources (MKT-03, CONTENT-05 verified)
4. Free resource detail page renders fully without login (GATE-01 verified)
5. Premium resources visible in catalog but detail content not rendered server-side (D-09 verified)
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-pipeline/03-06-SUMMARY.md`
</output>
